@model IEnumerable<WebApplication1.Sales_Person>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sales_Person_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sales_Person_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Sales_Person_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Sales_Person_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Sales_Person_ID })
        </td>
    </tr>
}

</table>
